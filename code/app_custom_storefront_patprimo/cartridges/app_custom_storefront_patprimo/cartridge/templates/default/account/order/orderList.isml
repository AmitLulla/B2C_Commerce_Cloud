<isloop items="${pdict.orders}" var="order">
    <div class="<isif condition="${pdict.action != 'Order-History'}">row</isif>">
        <div class="<isif condition="${pdict.action != 'Order-History'}">col-xs-12 col-sm-8</isif>">
            <isinclude template="account/order/orderHistoryCard"/>
        </div>
    </div>
</isloop>
